# dbt Models — OMOP CDM Transformations

dbt project for biomedical data platform. Run from this directory:

```bash
cd data_models/dbt
dbt deps
dbt run
dbt test
```

For backward compatibility, a symlink/copy exists at project root `dbt/` pointing here.
